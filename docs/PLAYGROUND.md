# Playground (this branch)

**Guest = explore. Account = complete Constitution. Subscription = Playground. Tier = new-law quota only.**

RecallC Playground is the paid learning overlay on verbatim Bare Act JSON. Tiers (`plus` / `pro` / `max`) differ only in 10 / 30 / unlimited new laws per quota period — not in learning quality.

| Document | Status |
|----------|--------|
| [PLAYGROUND_TWO_LAW_AUDIT.md](PLAYGROUND_TWO_LAW_AUDIT.md) | Overlay design. Cloze = architectural proof, not the finished mode set. |
| [PAYMENT_ENTITLEMENT_AUDIT.md](PAYMENT_ENTITLEMENT_AUDIT.md) | User-type entitlement + payment. **Not implemented.** See §21 decision gate for prices / Razorpay states / refunds. |

**On this branch (proof):** Add to Playground, section selection, Cloze, revision rows, guest → sign-in. No subscription gate. Cloze currently starts Day 1 (prototype).

**Finished train (not done):** user-type resolver (stop reading article claims for access), remaining RecallC-style modes, official Learned → then revision, Razorpay lifecycle, quota confirm, admin override. Open §21 cells block charging live; they do not restore article entitlements.
